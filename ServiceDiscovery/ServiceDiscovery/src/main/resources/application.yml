eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8761

spring:
  output:
    ansi:
      enabled: always
server:
  port: 8761